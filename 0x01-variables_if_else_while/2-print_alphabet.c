#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - print alphabets in lower case
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char letter = 'a'
		while (letter <= 'z')
		{
			putchar(letter);
			letter++;
		}
	putchar('\n');
	return (0);
}
