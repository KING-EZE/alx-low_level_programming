#include <stdio.h>

/**
 * main - fuction
 * @argc: count of strings
 * @argv: array of strings
 * Return: 0
 */

int main(int argc_attribute_((unused)), char *argv[])
{
	printf("%s\n", argv[]);
	return (0);
}
